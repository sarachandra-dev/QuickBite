<p>                                                                        <h1>QuickBite -- it is basically an food ordering app</h1></p> 

<h2>It is basically divided into to parts or logins</h2>
  
<h3>1) Client side :-</h3>

   -Generally it has user login access for the registered persons
   -Or the Signup for the new persons with email and password for login
   -user can access the food items and add to cart and buy them
   -It also has settings and direct access from menu page to cart page
   -And Other features in addition to it

<h3>TechStack:-</h3>

  <h4>-Frontend:-</h4>
    -in this we user gerneral html and css with javascript(.js) inside the (.ejs)
    <b>(ejs) basically means:-The core difference is that HTML, CSS, and JS are standard frontend technologies executed by the user's browser,
     whereas EJS is a backend templating tool used on a server to dynamically build those HTML pages before they are sent to the browser<b>

